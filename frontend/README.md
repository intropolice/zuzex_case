# 🐾 Цифровой Питомец - Next.js Frontend

Интерактивное веб-приложение для ухода за цифровым питомцем, созданное для хакатона Digital Pet.

## 🎯 Основные возможности

- ✨ Создание персонального цифрового питомца с уникальным именем
- 🎮 Интерактивные действия: покормить, поиграть, уложить спать, вылечить
- 📊 Отслеживание показателей питомца:
  - 🍗 Голод (сытость)
  - ⚡ Энергия (бодрость)
  - 😊 Настроение
- 🎨 Красивый адаптивный интерфейс с градиентами и анимациями
- 🔄 Автоматическое обновление состояния питомца каждые 30 секунд
- 📱 Полная поддержка мобильных устройств

## 🛠️ Технологический стек

- **Framework**: Next.js 14+
- **UI Library**: React 18+
- **Styling**: Tailwind CSS
- **Icons**: Lucide React
- **HTTP Client**: Axios
- **Language**: TypeScript

## 📋 Требования

- Node.js 18+
- npm или yarn
- Запущенный backend на `http://localhost:8000`

## 🚀 Установка и запуск

### 1. Установка зависимостей

```bash
npm install
```

### 2. Запуск приложения в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу [http://localhost:3000](http://localhost:3000)

### 3. Сборка для production

```bash
npm run build
npm start
```

## 📁 Структура проекта

```
frontend/
├── app/
│   ├── layout.tsx           # Главный layout приложения
│   ├── page.tsx             # Главная страница
│   ├── globals.css          # Глобальные стили
│   └── favicon.ico
├── components/
│   ├── GameDashboard.tsx    # Основной интерфейс игры
│   ├── PetAvatar.tsx        # Компонент аватара питомца
│   ├── PetStats.tsx         # Компонент отображения статистики
│   ├── PetCreationForm.tsx  # Форма создания питомца
│   └── ActionButtons.tsx    # Кнопки действий
├── context/
│   └── GameContext.tsx      # Context для управления состоянием игры
├── lib/
│   ├── api.ts              # API клиент для взаимодействия с backend
│   └── utils.ts            # Вспомогательные функции
├── types/
│   └── index.ts            # TypeScript типы
├── public/                  # Статические файлы
├── package.json
├── tsconfig.json
├── tailwind.config.js
├── postcss.config.js
└── next.config.js
```

## 🔌 API Endpoints

Приложение взаимодействует со следующими endpoints:

- `GET /pet` - Получить текущего питомца
- `POST /pet` - Создать нового питомца
- `POST /pet/feed` - Покормить питомца
- `POST /pet/play` - Поиграть с питомцем
- `POST /pet/sleep` - Уложить питомца спать
- `POST /pet/heal` - Вылечить питомца

## ⚙️ Конфигурация

### Переменные окружения

Создайте файл `.env.local`:

```bash
NEXT_PUBLIC_API_URL=http://localhost:8000
```

## 🎨 Стили и дизайн

- **Цветовая схема**: Фиолетово-синий градиент
- **Анимации**: Плавающие фигуры, анимация карточек, эффекты при наведении
- **Адаптивность**: Mobile-first подход, поддержка всех размеров экранов

## 📝 Компоненты

### GameProvider & useGame

Context для управления состоянием игры и питомца:

```tsx
const { pet, loading, error, initializePet, refreshPet } = useGame();
```

### GameDashboard

Основной интерфейс игры с отображением питомца и кнопками действий.

### PetCreationForm

Форма для создания нового питомца с введением имени.

### PetAvatar

Компонент отображения эмодзи питомца с анимацией.

### PetStats

Отображение статистики питомца в виде прогресс-баров.

### ActionButtons

Кнопки для выполнения действий с питомцем.

## 🐛 Troubleshooting

### "Cannot connect to API"
Убедитесь, что backend запущен на `http://localhost:8000` и переменная `NEXT_PUBLIC_API_URL` установлена корректно.

### "Pet not found"
Сначала создайте питомца через форму создания.

### Стили не загружаются
Убедитесь, что все зависимости установлены:
```bash
npm install
```

## 📧 Контакты и поддержка

Если у вас есть вопросы или предложения, создайте issue в репозитории.

## 📄 Лицензия

Проект создан для хакатона Digital Pet 2026.

---

**Создано для хакатона Digital Pet 🐾**
